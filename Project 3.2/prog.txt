program
{
  int c, a = 51, b = 6;
  string x = "abc";
  real y = 1.6765757;
  goto A;
  if (a >= 100)
  {
    read (y);
    write (y);
    write (x + y + z);
  }
  else
  {
    c = a = 21;
    break;
  }
A:
  while (c > 100)
  {
    c = c - 5;
    write (c);
    x = x + "step";
  }
  write (x);
  k = ( i /(b+2) * 2); /* ERROR – нет**  описания *переменной i */
  write ("ERROR not detected");
}